{
  "name": "cointab-project",
  "version": "1.0.0",
  "description": "cointab assigned project",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently \"cd api && yarn run dev\" \"cd client && yarn dev\" ",
    "setup": "cd api && npm install && cd ../client && yarn install"
  },
  "author": "mza-codes",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^7.6.0",
    "nodemon": "^2.0.20"
  }
}